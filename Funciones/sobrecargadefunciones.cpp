#include<iostream>

/*
La sobrecarga de funciones es una característica en C++ donde dos o más funciones pueden tener el mismo nombre pero diferentes parámetros. 
Cuando llamas a una función sobrecargada, el compilador de C++ determina la definición más apropiada a utilizar
comparando los tipos de argumentos que has usado en la llamada con los tipos de parámetros especificados en las definiciones.
 Si no se encuentra ninguna función coincidente, el compilador generará un error.
*/
  
