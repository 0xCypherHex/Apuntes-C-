#include<cstdlib>
#include<iostream>
#include<string>

int main () {

        // operador AND logico
     // el operador "&&" hace que se ejecute solo si ambos son verdaderos
    // 5 es mayor que 3 y 1 es igual a 1
    bool b1 = (5 > 3) && ( 1 == 1);
    
    // operador OR logico
    // basta con que una condicion sea verdadera para que se ejecute la sentencia    
    if (condicion1 == 6 || condicion2 == 7) {
        std::cout << "Es fin de semana";
    }


}